node_modules
.next
.env
.env.local
*.log
.DS_Store
*.tsbuildinfo

# Local-only build of the typed SDK, file-linked for verification until the
# npm package publishes (REA-4015). Never committed. See STAGING.md.
.local-typed-sdk
